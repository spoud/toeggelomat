import { LastMatchTimePipe } from './last-match-time.pipe';

describe('LastMatchTimePipe', () => {
  it('create an instance', () => {
    const pipe = new LastMatchTimePipe();
    expect(pipe).toBeTruthy();
  });
});
