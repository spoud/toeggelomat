query playerStats($seasonUuid: String!) {
  playerStats(seasonUuid: $seasonUuid) {
    uuid
    nickName
    wins
    losses
    winRate
    currentStreak
    goalDifference
    offensePoints
    defensePoints
  }
}
