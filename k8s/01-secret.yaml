# Placeholder only - do NOT apply this file as-is, and do NOT commit a real
# password here. Either edit the value below before applying, or (safer, so
# a real secret never touches git) create it imperatively instead and skip
# this file entirely:
#
#   kubectl --context spoud -n toeggelomat create secret generic toeggelomat-secret \
#     --from-literal=POSTGRES_PASSWORD="$(openssl rand -base64 24)"
#
apiVersion: v1
kind: Secret
metadata:
  name: toeggelomat-secret
  namespace: toeggelomat
type: Opaque
stringData:
  POSTGRES_PASSWORD: "changeme"
